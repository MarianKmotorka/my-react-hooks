import { useRef } from 'react'

//TODO: Implement
const useDoubleClick = (callback: () => void, maxTimeBetweenClicks = 300) => {
  const element = useRef<HTMLElement>()
  return element
}

export default useDoubleClick
